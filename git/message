<type>(<scope>): <summary>

# Why is this change needed?
# How does it address the issue?
# What side effects does this change have?

# Breaking Changes (if any):

# Closes/Fixes/Resolves: #issue_number

# --- Commit Message Guidelines ---
# <type>: feat|fix|docs|style|refactor|test|chore|perf|ci|build|revert
# <scope>: optional, e.g., (api), (ui), (auth)
# <summary>: imperative mood, e.g., "add", "fix", "update" (not "added", "fixed")
#
# - Keep first line under 50 characters
# - Wrap body at 72 characters
# - Use bullet points if needed
# - Reference issues and PRs in the footer
# -----------------------------------

# Types:
#   feat     (new feature)
#   fix      (bug fix)
#   docs     (documentation changes)
#   style    (formatting, missing semicolons, etc; no code change)
#   refactor (refactoring production code)
#   test     (adding tests, refactoring tests; no production code change)
#   chore    (updating build tasks, package manager configs, etc)
#   perf     (performance improvements)
#   ci       (continuous integration related)
#   build    (build system or external dependencies)
#   revert   (revert previous commit)

# Example:
# feat: add user authentication system
#
# Implement JWT-based authentication system with email verification.
# This allows users to securely log in and access protected routes.
#
# BREAKING CHANGE: Authentication required for all API endpoints
# Closes #123